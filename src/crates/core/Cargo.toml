[package]
name = "peakrunner-core"
version = "0.1.0"
edition = "2021"
publish = false

[features]
external-map = []

[dependencies]
glam = { version = "0.29", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
