# Local research — not release inputs

Private extracted assets are in `local-assets/`, captures in `screenshots/`,
reference bundles in `apps/`, and credentials in `secrets/`. Everything except
this README is Git-ignored. Do not force-add or publish it.

Build inputs live in `../src/`. Optional local links preserve research tool
paths without embedding imported maps in distributable binaries. Frozen app
launchers may reference historical paths; use the current client for normal play.

The pre-migration checkout is preserved at `~/workspace/PeakRunner.bak`.
